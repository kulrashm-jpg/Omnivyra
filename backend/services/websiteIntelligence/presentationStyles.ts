/**
 * Website Intelligence styling (Phase 20) — now Consumer #1 of the platform registry.
 * The single styling system lives in platformIntelligence/styles; this re-exports it so
 * every existing website import path stays byte-identical.
 */
export * from '../platformIntelligence/styles';
