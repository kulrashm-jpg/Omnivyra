/**
 * BARREL — content split into two modules (verbatim move, importers keep this path):
 *   reportHtmlSectionsCore — Report HTML sections — snapshot spec, subsections, foundational sections
 *   reportHtmlSectionsExtended — Report HTML sections — extended sections + spec assembly
 */
export * from './reportHtmlSectionsCore';
export * from './reportHtmlSectionsExtended';
