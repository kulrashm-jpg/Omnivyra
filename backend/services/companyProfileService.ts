/**
 * BARREL — content split into two modules (verbatim move, importers keep this path):
 *   companyProfileServiceCore — Company profile — types, normalization, field mapping
 *   companyProfileServiceRest1 — TEMP1
 */
export * from './companyProfileServiceCore';
export * from './companyProfileServiceRest1';
