{
  "baseline": 0,
  "note": "Non-regression ceiling: total `error TS` across the 3 typecheck-all projects (tsconfig.json / tsconfig.backend.json / tsconfig.scripts.json). CI FAILS if the count INCREASES. Lower this number (never raise it) as debt is genuinely fixed. Do NOT use suppressions/blanket any to game it. As of TYPECHECK-CLEAN-001 (2026-08-05) all three projects are CLEAN (0/0/0) and this baseline is 0 — any future `error TS` on these projects is a TRUE REGRESSION, not accepted debt. The previous 47 (TECH-DEBT register TD-001) were entirely tsconfig.scripts.json and were removed by fixing three root causes, with no suppressions, no `any`, and no compiler-option changes: (A) 9 scripts had no top-level import/export so TypeScript compiled them as GLOBAL scripts sharing one scope — declaring them modules with `export {};` removed 17 direct collisions (TS2451/TS2300/TS2393) plus 24 cascade errors where a colliding name resolved to another file's declaration; (B) scripts/ops/railwayEnvAudit.ts used a falsy test on a boolean-discriminated union, which does not narrow because the root tsconfig sets `strict: false` — switched to the repo-canonical `'reason' in x` structural narrowing (AuthorizationService.ts, legacyCookieSuperAdminBridge.ts, creatorAssetValidationService.ts); (C) scripts/verify-materialization.ts asserted CreatorTemplate (which has no index signature) directly to Record<string, unknown> — switched to the repo-canonical double assertion via `unknown`, and annotated `miss: string[]`, which Array.filter had otherwise inferred as the narrow literal union of REQUIRED. History: 86 as of main 7f5309f6 -> 47 (TECH-DEBT-001, 2026-07-26) -> 0 (TYPECHECK-CLEAN-001, 2026-08-05). SEPARATE SURFACE: `npm run typecheck:certification` guards tsconfig.backend.json and tsconfig.backend-tests.json via scripts/typecheck-certification-baseline.json; that surface still carries real pre-existing debt (443) under its own governed re-baselining process and is NOT covered by this file."
}
