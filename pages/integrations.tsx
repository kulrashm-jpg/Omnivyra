/** Page barrel — component + SSR props live in components/ (Next bans export * in pages). */
export { getServerSideProps } from '../components/integrationsSupportA';
export { default } from '../components/integrationsMain';
