# Repository Technical Debt Register

**Owner:** Repository Reliability · **Created:** 2026-07-26 (TECH-DEBT-001) · **Scope:** genuine technical debt — open items (P1–P3), plus **closure records** (marked *Resolved*) where a hygiene program explicitly logged the fix. Completed *product* work is not tracked here.

Priority: **P1** (address next milestone) · **P2** (planned) · **P3** (opportunistic). Effort: S (<0.5d) · M (0.5–2d) · L (>2d).

| ID | Title | Description | Impact | Owner | Priority | Recommended resolution | Effort | Target milestone |
|---|---|---|---|---|---|---|---|---|
| **TD-001** | `scripts/**` TypeScript debt — **RESOLVED** | Was **47** fenced type errors under `tsconfig.scripts.json`. Root-caused and eliminated by TYPECHECK-CLEAN-001 (2026-08-05), not patched: (A) 9 scripts had no top-level import/export so TypeScript compiled them as GLOBAL scripts sharing one scope — declaring them modules with `export {};` cleared 17 direct collisions (TS2451/TS2300/TS2393) and 24 cascade errors where a colliding name resolved to another file's declaration (e.g. `populate-infographic-previews.ts` was type-checking against `populate-carousel-previews.ts`'s `DEMO`); (B) `ops/railwayEnvAudit.ts` falsy-tested a boolean-discriminated union, which cannot narrow under the root `strict: false` — moved to the canonical `'reason' in x` form; (C) `verify-materialization.ts` asserted `CreatorTemplate` (no index signature) directly to `Record<string, unknown>` — moved to the canonical double assertion via `unknown`, plus a `string[]` annotation. No suppressions, no `any`, no config changes. | None — `typecheck-baseline.json` is now **0**, so new script debt can no longer hide under a non-zero ceiling. | Platform/DevEx | — | **Resolved (TYPECHECK-CLEAN-001, 2026-08-05):** baseline lowered **47 → 0**; all 3 `typecheck:ci` projects clean. | M | **Done** |
| **TD-002** | Backend **test-surface** type debt (443) | `tsconfig.backend-tests.json` carries 443 baselined errors (PB-009/PB-010). ts-jest is transpile-only, so tests aren't type-checked at run time; this project is the only gate. | Type errors can ship in test code with a green suite; slows confidence. | Backend | P2 | Burn down via `typecheck:certification` ratchet; re-baseline downward only. | L | Backend-quality |
| **TD-003** | No lightweight local run for infra-tier tests | Integration + Supabase-admin unit suites require the seeded **certenv** Supabase (`:54321/:54322`) / CI secrets. A bare `npx jest` shows ~50 infra failures easily mistaken for regressions. | DX / certification friction; risk of misattributing infra failures as code regressions. | Platform/DevEx | P2 | Document the seeded-env bootstrap (done: RELEASE-ENGINEERING-001 §3); consider a `test:unit-pure` script that selects the pure/mocked tiers. | M | DevEx |
| **TD-004** | Legacy `backend/tsconfig.json` (unreferenced) | A standalone `strict:true`, emitting (`dist`) backend config not referenced by any `package.json`/`scripts/`/CI. Creates "which tsconfig?" ambiguity. | Confusion; risk of certifying against the wrong config. | Platform | P3 | Document as legacy (done) **or** remove if no tool/IDE depends on it. | S | Hygiene |
| **TD-005** | Debug `console.log` in company-profile services | Pre-existing `console.log('Resolved company_id', …)` (Pulse) and `console.log('MERGED PROFILE', …)` (Competitors) ship to production logs. Not introduced by recent programs. | Log noise; minor PII surface (company_id). | Company Profile | P3 | Replace with structured observability or gate behind a debug flag. | S | Hygiene |
| **TD-006** | Stale backend certification baseline | `typecheck-certification-baseline.json` lists `tsconfig.backend.json: 1`; the one error (`pages/api/company-profile/index.ts` TS2322) was fixed in BASELINE-HYGIENE-003 → actual is now 0. | Baseline no longer tight; a re-introduced error up to 1 would pass. | Backend | P2 | Run `npm run typecheck:certification:baseline` to lock `tsconfig.backend.json` → 0 (dedicated commit). | S | This milestone |
| **TD-007** | `market_substitute` synthetic alternatives | Competitor engine still emits a hardcoded "market alternative" ("Life coaches and clarity consultants", `competitorEngineServiceEngineDiscovery.ts`). Pre-existing; **partitioned out of competitors** (never shown as a competitor). **Out of scope here (do not modify Competitor Intelligence).** | Cosmetic; a residual synthetic entity for full no-fabrication purity. | Competitor Intel | P3 | Evaluate replacing with evidence-derived alternatives, or gating, in a dedicated Competitor-Intel follow-up. | M | Competitor-Intel follow-up |
| **TD-008** | Documentation Canonicalization — **RESOLVED** | Historical duplication of TypeScript certification documentation: `RELEASE-ENGINEERING-001` restated the tsconfig-projects/baseline tables already owned by the authoritative `docs/TYPESCRIPT-VALIDATION-STRATEGY.md`, and TECH-DEBT-001's baseline reductions (86→47; tsconfig.json 6→0; backend 1→0) left stale figures in older PMO records. | Ambiguity over the canonical guide; drifting/contradictory published numbers. | Repository Docs | — | **Resolved (DOC-HYGIENE-001, 2026-07-26):** designated `TYPESCRIPT-VALIDATION-STRATEGY.md` canonical; re-scoped `RELEASE-ENGINEERING-001` as a companion (tsconfig/baseline tables replaced by a pointer); updated the canonical's current numbers to 47/0/0/443 and centralized the historical-record reconciliation there. | S | **Done** |

**Not debt (explicitly excluded):** completed programs (Competitor Intelligence, Conversation Intelligence, Strategic Recommendation Intelligence), the resolved root-frontend TS errors (fixed in TECH-DEBT-001), and infra-tier test failures that are green in the seeded environment.
