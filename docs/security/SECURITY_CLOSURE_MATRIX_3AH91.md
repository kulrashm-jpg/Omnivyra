# Security closure matrix — STEP 3AH-91

**Base:** `main @ f44b1387` (PR #245 merged + live). One row per finding across workstreams SEC-A … SEC-F.
This skeleton carries the SEC-F (security gates / verification) rows; the orchestrator merges the other workstreams' rows.
Status vocabulary: `OPEN`, `FIXED (branch)`, `ALREADY FIXED`, `FALSE POSITIVE`, `INTENTIONALLY ACCEPTED`, `MANUAL OPERATION REQUIRED`, `TRACKED (gate)`.

| ID | Severity | Finding | Original status | Current status | Fix/PR | Tests | Production status | Remaining action | Verdict |
|---|---|---|---|---|---|---|---|---|---|
| SEC-F1 | P2 | Route-auth gate decided R1 per file, so an unauthenticated method branch in an otherwise-authenticated route passed | OPEN (ROUTE_AUTH_001 §7.1) | FIXED (branch `sec/3ah91-f-gates`): R1-METHOD, 260 dispatching routes checked per branch, reviewed `methodExemptions` re-verified each run | sec/3ah91-f-gates (unmerged) | `sec91FRouteAuthMethod.test.ts` 23/23; mutation → 11 fail | CI-only (no runtime change) | Merge; keep the gate blocking | CLOSED on merge |
| SEC-F1-01 | P3 | `pages/api/accounts/[platform].ts:76` POST branch unauthenticated (mock connect; logs caller `code` at :81) | Unknown (found by R1-METHOD) | OPEN — tracked by a `stub` method exemption (no DB/await/import call, re-verified) | — (SEC-A) | repo assertion in `sec91FRouteAuthMethod.test.ts` | Live, reachable anonymously; no data access | SEC-A: authenticate or answer 410; drop `code` logging; remove the exemption | OPEN → SEC-A |
| SEC-F1-02 | P3 | `pages/api/track/angle-industry-matrix.ts:53` GET unauthenticated service-role read of the platform-wide aggregate | Unknown (found by R1-METHOD) | OPEN — tracked by a `public` method exemption (never writes, re-verified) | — (SEC-A) | repo assertion | Live; aggregate, no tenant columns | SEC-A: require identity; remove the exemption | OPEN → SEC-A |
| SEC-F1-03 | P3 | Same route POST (`:93-139`): any member of any company writes a cross-tenant global aggregate | Unknown | OPEN | — (SEC-A) | — | Live (integrity only) | SEC-A: restrict to platform/service path or scope per company | OPEN → SEC-A |
| SEC-F2 | P2 | Migration gate: earlier-timestamp bypass; definer functions still EXECUTE-able by `authenticated`; no view/MV rule; GRANT / DEFAULT PRIVILEGES / DISABLE RLS / ALTER FUNCTION / ALTER POLICY unchecked; `TO authenticated USING (true)` passed; DO-block tables invisible | OPEN | FIXED (branch): frozen ordering snapshot (415, floor 20261026000000) + strengthened rules (a)–(f) with reviewed annotations | sec/3ah91-f-gates (unmerged) | `sec91FMigrationQuality.test.ts` 30/30 + `migrationQualitySecurityRules.test.ts` 16/16; mutation → ordering tests fail | CI-only; no migration added or applied | Merge; SEC-C's migration must satisfy the rules | CLOSED on merge |
| SEC-F3 | P3 | SSRF scanner: backend + pages/api only, bare identifiers only, all template literals and `const u = \`https:…\`` exempt | OPEN | FIXED (branch): lib/** server modules, dynamic-host templates, host-fixing const rule; 11 new call sites on main: 3 resolved by analysis, 7 annotated `// ssrf-ok:`, 1 KNOWN OPEN | sec/3ah91-f-gates (unmerged) | `sec91FSsrfScanner.test.ts` 16/16 + `ssrfCiGuard.test.ts`; mutation → 5 fail | CI-only | Merge | CLOSED on merge |
| SEC-F3-KO | P2 | `pages/api/campaigns/[id]/propose-frequency-rebalance.ts:91` server-side fetch to request-derived Origin forwarding credentials | OPEN (ROUTE_AUTH_001 §7.2) | TRACKED (gate `KNOWN_OPEN`, printed every run; WARN when fixed) | — (SEC-E) | repo assertion in `sec91FSsrfScanner.test.ts` | Live | SEC-E fix; then delete the `KNOWN_OPEN` entry | OPEN → SEC-E |
| SEC-F4 | P2 | No secret-pattern gate although real credentials reached history | OPEN | FIXED (branch): `scripts/check-secrets.js` (15 patterns + tracked dotenv), fingerprint-pinned allowlist (1 entry), never prints values | sec/3ah91-f-gates (unmerged) | `sec91FSecretsGate.test.ts` 23/23; mutation → 2 fail | CI-only; history not rewritten | Merge. Rotation of historically leaked credentials is owned by SEC-B / owner (MANUAL) | CLOSED on merge (new commits); history = see SEC-B |
| SEC-F5 | P3 | `check-tenant-authz.js` blind spots (comment-satisfied call, campaignId invisible, per-file) | OPEN | DOCUMENTED — superseded by the route-auth gate, retained (still blocking) | sec/3ah91-f-gates (unmerged) | `sec91FGateHardening.test.ts` F5 4/4 | n/a | none | INTENTIONALLY ACCEPTED (superseded) |
| SEC-F6 | P2 | 31 dormant routes (D16 / D2 5 / F10) could silently regress | OPEN | FIXED (branch): verdict pins + 9 pre-3AH-85 shape fixtures at the real paths | sec/3ah91-f-gates (unmerged) | `sec91FDormantRoutes.test.ts` 41/41 | CI-only | Merge | CLOSED on merge |
| SEC-F7 | P2 | New/strengthened gates not guaranteed blocking in CI | OPEN | FIXED (branch): `typecheck-baseline.yml` steps “Secret-pattern gate” + “Security gate fixture tests” (inventory-pinned runner); `check:secrets`, `test:security-gates` scripts; no step removed | sec/3ah91-f-gates (unmerged) | `sec91FGateHardening.test.ts` F7 11/11 | CI-only | Merge; mark the job required if not already | CLOSED on merge |
| SEC-F8 | — | Closure matrix | — | This document | sec/3ah91-f-gates | — | — | Orchestrator merges SEC-A…E rows | — |
