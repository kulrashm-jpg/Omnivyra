/**
 * Company Context Block — Centralized Enforcement Infrastructure.
 *
 * BARREL — verbatim Agent-B split into 3 parts (importers keep this path):
 * Builders (types/identity/prompt blocks), Scoring (company-context scoring),
 * Enforcement (blocking helpers + section-level validation).
 */
export * from './companyContextBlockBuilders';
export * from './companyContextBlockScoring';
export * from './companyContextBlockEnforcement';
